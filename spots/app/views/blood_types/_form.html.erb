<%= form_for(@blood_type) do |f| %>
  <% if @blood_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blood_type.errors.count, "error") %> prohibited this blood_type from being saved:</h2>

      <ul>
      <% @blood_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
