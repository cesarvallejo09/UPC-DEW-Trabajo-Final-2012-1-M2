<h1>Listing blood_types</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Descripcion</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @blood_types.each do |blood_type| %>
  <tr>
    <td><%= blood_type.nombre %></td>
    <td><%= blood_type.descripcion %></td>
    <td><%= link_to 'Show', blood_type %></td>
    <td><%= link_to 'Edit', edit_blood_type_path(blood_type) %></td>
    <td><%= link_to 'Destroy', blood_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Blood type', new_blood_type_path %>
