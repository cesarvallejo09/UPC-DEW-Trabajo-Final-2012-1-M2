<%= form_for(@invoice, :html => { :multipart => true }) do |f| %>
  <% if @invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
      <% @invoice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table class="tabla_repeater">
	<tr>
		<th><%= f.label :fecha %></th>
		<td><%= f.date_select :fecha %></td>
	</tr>

	<tr>
		<th><%= f.label :subtotal %></th>
		<td><%= f.text_field :subtotal %></td>
	</tr>
	
	<tr>
		<th><%= f.label :igv %></th>
		<td><%= f.text_field :igv %></td>
	</tr>

	<tr>
		<th><%= f.label :total %></th>
		<td><%= f.text_field :total %></td>
	</tr>

	<tr>		
		<td colspan="2" align="center"><%= f.submit %></td>
	</tr>
	
<% end %>
